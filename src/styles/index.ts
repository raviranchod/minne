import { colors } from "./colors";
import { common } from "./common";
import { fonts } from "./fonts";
import { layout } from "./layout";

const theme = {
  colors,
  common,
  fonts,
  layout,
};

export { theme };
